# 施工场地火焰监测系统需求报告

## 1. 项目背景

本项目基于已训练完成的 YOLOv8 火焰识别模型，开发一套面向施工场地的视频火焰监测系统。系统面向施工现场安全监控场景，能够在视频画面或实时摄像头画面中自动识别疑似火焰目标，在前端页面中实时框选火焰位置并进行预警提示，同时在后端对火情事件及截图图片进行统一管理。

系统整体保持简约、实用，优先满足核心监测与管理需求。

## 2. 建设目标

- 基于已有 YOLOv8 火焰检测模型，对施工场地视频进行自动分析。
- 支持两种独立的检测方式：上传视频分析和本地摄像头实时监控，两种方式的结果完全分离。
- 实时摄像头监控场景下，前端能够实时框选火焰位置，并在页面中进行预警提示。
- 视频分析场景下，系统对上传视频进行离线分析，返回火情事件和截图。
- 后端能够保存火情事件及对应截图图片，支持基础查询和删除管理。
- 系统概览页面仅展示摄像头来源的火情数据。
- 整体架构采用前后端分离模式，前端使用 Vue，后端使用 Java，推理服务使用 Python。

## 3. 系统范围

### 3.1 本期已实现

- 上传视频文件并同步分析（源类型 `UPLOAD_VIDEO`）
- 本地摄像头实时监控，自动录制，间隔采样分析，Canvas 实时框选火焰（源类型 `CAMERA`）
- 视频分析事件与摄像头监控事件完全分离管理
- 火情事件保存、查询、详情、删除
- 火情截图保存与管理
- 系统概览页面（仅展示摄像头火情数据）
- 已上传视频文件的清空管理

### 3.2 本期暂不包含

- 短信、邮件、电话等外部通知
- 多级权限审批流程
- 多组织、多租户管理
- 复杂统计分析与 BI 报表
- 大规模分布式调度与高可用集群
- 用户登录与角色权限（后续扩展）

## 4. 用户角色

### 4.1 管理员

负责查看火情记录、删除无效数据、清空已上传视频文件。

### 4.2 值班监控人员

负责在前端页面进行实时摄像头监控、上传视频分析、查看历史火情记录和图片。

本期暂不实现细粒度权限控制，采用简化模式。

## 5. 业务场景

### 5.1 实时摄像头监控场景

用户在前端页面开启摄像头，系统自动开始录制并每 3 秒截取一帧画面进行 YOLO 火焰检测。检测到火焰时，前端在视频画面上实时绘制红色框选标注火焰位置，并在页面顶部显示红色闪烁告警。同时，火情截图和事件记录自动保存到后端，系统概览页面数据同步更新。

### 5.2 上传视频分析场景

用户通过前端上传施工现场历史视频文件，系统同步调用推理服务进行逐帧分析。分析完成后，返回检测结果，识别出的火情事件使用视频内时间码（HH:mm:ss）记录，截图和事件数据保存到后端，供后续查询。

## 6. 功能需求

### 6.1 系统概览

- 展示今日摄像头火情数量（仅统计 `CAMERA` 来源的事件）。
- 展示最近一次摄像头火情的事件编号、发生时间、置信度、截图链接。
- 无摄像头火情时显示空状态提示。
- 两个卡片区域保持适当间距。

### 6.2 视频分析

- 用户可在前端选择本地视频文件。
- 点击「开始分析」后上传视频，系统同步分析并返回结果。
- 分析完成后自动刷新事件列表，无需手动操作。
- 视频事件使用视频内时间码（HH:mm:ss）记录发生时间，而非实际上传时间。
- 事件编号格式为 `VEVT` 前缀。
- 事件列表展示事件编号、视频时间码、帧号、置信度，支持查看详情和删除。
- 提供「清空视频文件」功能，可删除服务器上所有已上传的视频文件（需二次确认）。

### 6.3 实时摄像头监控

- 用户点击「开启摄像头」后，系统自动获取摄像头画面并开始录制。
- 摄像头状态通过 `useCameraMonitor` composable 管理，具有跨页面持久性——用户切换到其他页面时，摄像头录制和分析不会中断，返回实时监控页面后自动重新绑定视频画面。
- 只有手动点击「关闭摄像头」按钮才会停止录制和分析。
- 系统每 3 秒自动截取一帧画面发送到后端进行 YOLO 检测。
- 后端返回检测框坐标（x1, y1, x2, y2, confidence），前端在视频画面上用 Canvas 覆盖层实时绘制红色半透明矩形框标注火焰位置。
- 框选区域上方显示火焰类别和置信度百分比标签（如 `fire 92.3%`）。
- 检测到火情时，页面顶部显示红色闪烁告警文字，包含置信度信息。
- 未检测到火情时，页面顶部显示绿色"监控中"状态。
- 视频画面左上角显示"实时分析中"徽章。
- 录制的视频支持页面内回放。
- 检测到火情时自动保存事件和截图，事件编号格式为 `CEVT` 前缀，使用实际时间记录。
- 检测到新火情时自动刷新事件列表（通过 composable 的事件回调机制）。
- 摄像头监控数据自动同步到系统概览页面。

### 6.4 火情事件管理

- 每次火情识别生成独立事件记录。
- 视频分析事件与摄像头监控事件完全分离，互不干扰。
- 事件信息包含：事件编号、来源类型、来源名称、发生时间、置信度、截图地址、视频时间码（视频分析）、帧号。
- 支持按来源类型筛选查询。
- 支持查看事件详情（弹窗展示完整信息和截图预览）。
- 支持删除单条事件记录（需二次确认）。

### 6.5 火情截图管理

- 对每次识别出的火情保存带标注框的截图图片。
- 截图文件存储在服务器 `storage/snapshots/fire/` 目录。
- 支持截图的查询和删除。

## 7. 非功能需求

### 7.1 易用性

- 界面风格简洁清晰，适合值班人员快速识别风险。
- 左侧导航栏包含三个页面入口：系统概览、视频分析、实时监控。
- 操作流程尽量控制在少量步骤内完成。
- 关键状态有明确文字和视觉提示。

### 7.2 性能要求

- 实时摄像头监控场景下，每 3 秒完成一次帧分析，具备较好的实时性。
- 上传视频分析场景下，同步等待分析完成，直接返回结果。
- 视频分析采样频率为每秒 3 帧，同一火情事件间隔至少 3 秒。
- 对中小规模场景，支持单机稳定运行。

### 7.3 可维护性

- 前后端分离，接口边界清晰。
- 模型推理能力作为独立 Python 服务封装，便于后续替换模型。
- 后端保留基础日志，便于问题排查。

### 7.4 可扩展性

- 后续可扩展更多告警方式，如短信、邮件、企业微信等。
- 后续可扩展更多事件字段，如误报标记、处理状态。
- 后续可扩展多摄像头、多工地区域接入。
- 后续可引入 WebSocket 实时推送替代轮询。

## 8. 技术约束

- 前端采用 Vue 3 + Vite + Element Plus 技术栈。
- 后端采用 Java 17 + Spring Boot 3 + Spring Data JPA + MySQL 8。
- 推理服务采用 Python + FastAPI + Ultralytics YOLOv8。
- 系统采用前后端分离架构，三服务独立部署。
- Python 虚拟环境通过 uv 管理，启动时使用 `source .venv/bin/activate` 激活后直接 `python` 命令。
- Java 版本通过 jenv 管理，需确保 JAVA_HOME 指向 Java 17。
- 本期不引入 Redis、MQ、对象存储等组件，保持部署简单。

## 9. 业务流程

### 9.1 实时摄像头监控流程

1. 用户在前端「实时监控」页面点击「开启摄像头」。
2. 浏览器请求摄像头权限并获取视频流。
3. `useCameraMonitor` composable 在模块级别管理摄像头状态，创建隐藏 `<video>` 元素用于帧截取。
4. 系统自动开始录制（MediaRecorder API）。
5. 系统每 3 秒自动截取一帧，转为 Base64 发送到后端。
6. 后端转发到 Python 推理服务进行 YOLO 检测。
7. 推理服务返回检测框坐标和置信度。
8. 若检测到火情：推理服务保存带标注框的截图，后端保存任务和事件记录。
9. 后端返回结果给前端。
10. 前端在视频画面上用 Canvas 绘制火焰框选，更新告警状态。
11. composable 触发 `onFireDetected` 回调，事件列表自动刷新。
12. 系统概览页面数据同步更新。
13. 用户切换到其他页面时，摄像头录制和分析不中断。
14. 返回实时监控页面后，自动重新绑定视频画面和 Canvas 框选。
15. 用户手动点击「关闭摄像头」时停止录制和分析。

### 9.2 上传视频分析流程

1. 用户在前端「视频分析」页面选择本地视频文件。
2. 点击「开始分析」，视频文件上传到后端。
3. 后端保存视频文件到 `storage/uploads/videos/`。
4. 后端创建分析任务（sourceType=UPLOAD_VIDEO）。
5. 后端同步调用 Python 推理服务分析视频。
6. 推理服务逐帧检测（每秒 3 帧），保存火情截图，使用视频时间码。
7. 推理服务返回事件列表。
8. 后端保存火情事件（eventNo 前缀 VEVT），返回结果给前端。
9. 前端自动刷新事件列表，展示分析结果。

## 10. 数据需求

### 10.1 检测任务

- 任务编号（task_no）：业务唯一标识
- 任务类型（task_type）：CAMERA / VIDEO
- 来源类型（source_type）：CAMERA / UPLOAD_VIDEO
- 来源名称（source_name）
- 任务状态（status）：CREATED / RUNNING / FINISHED / FAILED / STOPPED
- 视频路径（video_path）：上传视频的存储相对路径
- 已处理帧数（frame_count）
- 火情次数（fire_count）
- 结果摘要（result_summary）
- 失败原因（error_message）
- 开始时间、结束时间、创建时间、更新时间

### 10.2 火情事件

- 事件编号（event_no）：VEVT 前缀为视频事件，CEVT 前缀为摄像头事件
- 关联任务 ID（task_id）
- 来源类型（source_type）：CAMERA / UPLOAD_VIDEO
- 来源名称（source_name）
- 发生时间（event_time）：视频分析为视频时间码对应的 datetime，摄像头为实际时间
- 置信度（confidence）
- 持续时长（duration_seconds）
- 截图路径（snapshot_path）
- 帧号（task_frame_no）
- 视频时间码（video_timecode）：HH:mm:ss 格式，仅视频分析类型有效
- 备注（remark）
- 创建时间、更新时间

### 10.3 火情图片

- 关联事件 ID（event_id）
- 文件名（file_name）
- 文件路径（file_path）
- 文件访问 URL（file_url）
- 截图时间（capture_time）
- 来源类型（source_type）
- 文件大小（file_size）
- 创建时间、更新时间

## 11. 验收要点

- 能够接入本地摄像头并实时监控，自动录制并间隔分析。
- 实时监控时能够在视频画面上框选火焰位置。
- 检测到火情后，页面能够显示红色闪烁告警。
- 能够上传视频并完成同步离线分析。
- 视频分析事件使用视频时间码，与摄像头事件完全分离。
- 系统概览页面仅展示摄像头来源的火情数据。
- 后端能够保存火情截图及事件记录。
- 前端能够查看历史火情记录并查看详情和截图。
- 前端能够删除指定事件记录。
- 能够清空已上传的视频文件。
- 系统整体界面与流程简洁可用。
- 摄像头监控在切换页面时保持不中断，返回后自动恢复画面和框选。

## 12. 后续扩展建议

- 用户登录与角色权限
- 告警消息外发（短信、邮件、企业微信）
- 误报人工标注与确认
- 火情处置流程跟踪
- 数据统计与报表分析
- 多摄像头集中管理
- WebSocket 实时推送替代轮询
- 录像视频保存到服务器端
